export const metadata = {
  title: "Installation | Sunya",
  description: "Install Sunya for static analysis of EVM smart contracts",
}

# Installation

Install Sunya with curl. No Node.js or npm required.

```bash
curl -sSL https://raw.githubusercontent.com/13x54n/sunya-v1/main/package/bin/install | sh
```

Add `~/.local/bin` to your PATH if needed:

```bash
export PATH="$HOME/.local/bin:$PATH"
```

Then run `sunya scan` in a project with Solidity contracts.

## Commands

- `sunya scan [dir]` — Analyze contracts (default: ./contracts or ./src)
- `sunya install` — Install or update Slither
- `sunya uninstall [-y]` — Remove sunya from your system (use `-y` to skip confirmation)

## Uninstall

```bash
sunya uninstall      # Prompts for confirmation
sunya uninstall -y   # Remove without prompting
```

## Requirements

- Bash
- Python 3 (for Slither)
